# Saver Backend 작업 지침

## 프로젝트 역할

- 이 저장소는 포털 서비스 **Saver**의 backend를 관리한다.
- Saver는 검색, 날씨 등 여러 정보를 제공하지만, backend가 외부 정보의 수집이나 검색 결과 생성을 직접 담당하지 않는다.
- backend는 frontend의 단일 진입점으로서 요청을 검증하고, 적절한 서비스에 작업을 전달하며, 준비된 결과를 사용자에게 반환한다.
- backend는 사용자 인증과 연계된 간단한 블로그 기능도 직접 제공한다. 블로그는 게시글 작성·조회·수정·삭제 등 기본 기능에 집중하고 불필요하게 복잡한 기능을 추가하지 않는다.
- 주요 기술은 FastAPI, PostgreSQL, Redis, RabbitMQ이다.

## 검색 기능의 책임 경계

- backend는 외부 검색 엔진 API를 직접 호출하면 안 된다.
- backend는 검색 결과를 직접 생성하거나 가공하기 위해 외부 검색 서비스에 동기 요청을 보내면 안 된다.
- 외부 검색 엔진 API 호출과 검색 결과 생성은 별도의 검색 API 관리 저장소가 담당한다.
- backend가 사용자에게 반환하는 검색 상태와 검색 결과의 유일한 데이터 원천은 Redis이다.
- RabbitMQ는 검색 작업 명령을 전달하는 용도로만 사용한다. RabbitMQ 메시지 ACK를 검색 결과 자체나 검색 완료를 보장하는 응답으로 취급하지 않는다.

## 검색 작업 메커니즘

검색은 다음 두 API 단계로 분리한다.

### 1. 검색 요청 접수

사용자가 검색어를 보내면 backend는 다음 순서를 지킨다.

1. 검색 요청과 검색어를 검증한다.
2. 해당 검색을 허용하고 이후 요청을 식별하기 위한 추측하기 어려운 `magicCode`를 생성한다.
3. 사용자에게 `magicCode`를 반환한다. 이 응답에는 외부 검색 결과를 포함하지 않는다.
4. 검색어를 정규화한 뒤 안정적인 방식으로 해시하여 검색어 해시 키를 만든다. 같은 검색어가 같은 키를 사용하도록 정규화 및 해시 규칙을 일관되게 유지한다.
5. Redis에 `magicCode` 키를 생성하고 초기 상태 `PENDING`과 검색어 해시 키에 대한 참조를 저장한다.
6. Redis에 검색어 해시 키를 생성하거나 필요한 초기 상태를 설정한다. 검색 결과는 검색어 해시 키를 기준으로 공유하거나 조회할 수 있어야 한다.
7. Redis 초기화가 성공한 후 RabbitMQ에 검색 명령 메시지를 발행한다. 메시지에는 최소한 외부 검색 작업자가 검색을 수행하고 대응 Redis 항목을 갱신하는 데 필요한 `magicCode`, 검색어 또는 합의된 검색어 표현, 검색어 해시 키를 포함한다.
8. Frontend에서 이 magicCode를 가지고 polling 요청을 보낼 것이라고 기대할 수 있으며, 검색 결과를 가져올 수 있을 때 검색 결과를 반환해야 한다.

Redis 기록과 RabbitMQ 발행 사이의 부분 실패를 고려해야 한다. 발행에 실패했는데 요청을 정상 접수한 것처럼 응답해서는 안 되며, 재시도 시 중복 작업이 발생해도 안전하도록 메시지와 상태 변경을 멱등적으로 설계한다. 키에는 운영 정책에 맞는 TTL을 설정하여 사용되지 않는 `PENDING` 상태와 검색 결과가 무기한 남지 않게 한다(예: `magicCode` 범주 key는 1분, `검색어 해시`는 범주 key는 3분).

### 2. 실제 검색 결과 조회

사용자는 앞 단계에서 받은 `magicCode`로 backend의 검색 결과 API를 호출한다. backend는 다음 순서를 지킨다.

1. `magicCode`의 형식과 유효성을 검증한다.
2. Redis에서 `magicCode` 키를 조회한다.
3. 키가 없거나 만료되었거나 유효하지 않으면 검색 작업을 새로 만들지 말고 적절한 4xx 응답을 반환한다.
4. 상태가 `PENDING`이면 HTTP `202 Accepted`를 반환한다.
5. 검색 작업자가 Redis를 갱신하여 결과가 준비된 경우, `magicCode`가 참조하는 검색어 해시 키를 통해 결과를 읽고 HTTP `200 OK`로 반환한다.
6. 명시적인 실패 상태가 저장되어 있다면 해당 실패를 안정적인 오류 응답으로 변환한다. 내부 오류 내용이나 민감한 메시지를 그대로 노출하지 않는다.

조회 API는 Redis에 아직 결과가 없다는 이유로 외부 검색 엔진을 호출하거나 RabbitMQ에 새 검색 명령을 발행하면 안 된다. 조회는 상태와 결과를 읽는 작업이어야 한다.

## 외부 검색 작업자와의 계약

- 별도 저장소의 검색 작업자는 RabbitMQ에서 검색 명령을 소비한다.
- 작업자는 검색을 처리한 후 합의된 스키마에 따라 Redis의 `magicCode` 상태와 검색어 해시 키의 결과를 갱신한다.
- 작업자는 필요한 Redis 갱신이 성공한 뒤 메시지를 ACK해야 한다. 처리에 실패했을 때는 재시도 및 dead-letter 정책에 맞게 NACK 또는 실패 처리를 수행한다.
- backend는 ACK를 직접 기다려 사용자 요청을 블로킹하지 않는다. 결과 조회 시점에 Redis에 기록된 값을 기준으로 `202`, `200` 또는 오류 응답을 결정한다.
- 메시지 필드, Redis 키 형식, 상태 값, 결과 스키마를 변경할 때는 검색 API 관리 저장소와의 호환성을 먼저 확인한다.

## 구현 원칙

- 네트워크 및 저장소 I/O는 FastAPI 요청 처리를 불필요하게 막지 않도록 비동기 호환 클라이언트와 연결 수명주기를 사용한다.
- 기존 엔드포인트를 변경하거나 새 엔드포인트를 추가할 때는 `openapi.json`에 목적, 입력값, 인증 여부, 정상 응답과 주요 오류 응답이 드러나도록 FastAPI의 summary, description, response model, status code 및 responses 문서를 상세히 작성한다.
- 카카오 액세스 토큰은 로그인 및 탈퇴 요청 중 필요한 카카오 API 호출에만 사용하고 클라이언트 쿠키나 저장소에 남기지 않는다. 로그인 상태는 사용자 ID와 만료시각을 담은 HttpOnly HMAC 서명 쿠키로 유지하며, 서명 비밀값을 로그나 저장소에 노출하지 않는다.
- 계정 탈퇴 시 카카오 계정을 재인증하고 provider 연결 끊기가 성공한 뒤 로컬 사용자와 사용자가 소유한 데이터를 즉시 삭제한다. provider 액세스 토큰은 이 요청 중에만 사용하며 저장하지 않는다. 모든 인증 요청은 서명뿐 아니라 사용자 행의 존재도 확인하여 탈퇴 후 남은 쿠키를 거부한다.
- `magicCode`는 권한 증표 역할을 하므로 충분한 엔트로피를 가진 안전한 난수로 만들고 로그에 원문을 남기지 않는다.
- 검색어 원문, 인증 정보, Redis/RabbitMQ 접속 정보 등 민감할 수 있는 값은 로그에 기록하지 않는다.
- Redis 상태 변경이 여러 필드 또는 키에 걸치면 transaction, Lua script 등 원자성을 보장할 방법을 검토한다.
- 사용자 입력, 메시지 payload, Redis에서 읽은 데이터는 모두 신뢰하지 말고 경계에서 검증한다.
- 새 검색 코드와 변경 사항에는 최소한 다음 경우를 검증하는 테스트를 작성한다: 정상 접수, RabbitMQ 발행 실패, 유효하지 않거나 만료된 `magicCode`, `PENDING` 조회, 완료 결과 조회, 외부 작업 실패 상태, 중복 요청 및 재시도.

## 사용자 인증 및 데이터베이스 예외 처리

- 서명된 세션 쿠키를 사용하는 모든 인증 요청은 쿠키의 서명과 만료시각을 검증한 뒤 데이터베이스에서 사용자 행의 존재도 확인한다. 쿠키가 없거나 유효하지 않거나 사용자 행이 없으면 `401 Unauthorized`를 반환한다.
- 데이터베이스 연결 획득과 쿼리 실행은 하나의 `try` 블록으로 감싸고, `asyncpg.PostgresError`, `asyncpg.InterfaceError`, `TimeoutError`, `OSError`처럼 저장소 장애를 나타내는 예외만 명시적으로 처리한다. `Exception`을 포괄적으로 잡아 프로그래밍 오류나 애플리케이션 오류를 저장소 장애로 위장하지 않는다.
- 데이터베이스 장애는 도메인별 저장소 오류 변환 함수를 통해 `503 Service Unavailable`로 변환하고 `raise ... from exc`로 예외 연결을 보존한다. 인증 데이터 조회 중 발생한 저장소 장애를 잘못된 세션으로 간주하여 `401`로 반환하면 안 된다.
- 저장소 오류 로그에는 작업을 구분할 수 있는 고정된 식별자와 예외 클래스명만 남긴다. SQL, 쿼리 인자, 사용자 인증 정보, 접속 정보 및 원본 예외 메시지는 로그나 API 응답에 노출하지 않는다.
- 사용자나 데이터가 존재하지 않는 것처럼 예상 가능한 결과는 데이터베이스 예외 처리 블록 밖에서 `401` 또는 `404` 등 해당 의미에 맞게 처리한다.
- 데이터베이스를 사용하는 엔드포인트의 OpenAPI `responses`에는 `503`을 명시하고, 테스트에서 저장소 예외가 `503`으로 변환되며 내부 예외 메시지가 응답에 포함되지 않는지 검증한다.

## Git 작업 규칙

- 이 프로젝트는 Git으로 관리한다.
- 현재 `week/1` 브랜치만 존재하는 것은 의도된 초기 상태이다. 이를 기본 브랜치 구성 오류로 간주하거나 임의로 브랜치를 변경하지 않는다.
- 이후 `week/1`은 `main`에 병합될 수 있고 다른 브랜치가 추가될 수 있으므로, 작업할 때 항상 현재 브랜치와 대상 브랜치를 확인한다.
- 사용자의 기존 변경 사항을 보존하고, 관련 없는 파일을 되돌리거나 커밋에 섞지 않는다.
- 커밋은 하나의 논리적 변경 단위로 구성한다.
- 커밋 메시지는 반드시 한국어로 상세하게 작성한다. 제목에는 변경 목적을 명확히 쓰고, 본문에는 주요 구현 내용과 필요한 경우 검증 방법 및 영향 범위를 설명한다.
- 사용자가 명시적으로 요청하지 않은 커밋, push, merge, rebase 또는 브랜치 생성은 수행하지 않는다.
